import {StyleSheet} from 'react-native';

const toDoListDetails = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'column',
    margin: 10,
    flex: 1,
  },
});

export default toDoListDetails;
